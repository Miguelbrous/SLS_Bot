# Variables comunes
# Copia este archivo a `.env` y ajusta los valores sin subirlos al repositorio.

# --- Backend FastAPI (bot/sls_bot) ---
PORT=8080
HOST=0.0.0.0
SLSBOT_CONFIG=./config/config.json
SLSBOT_MODE=demo
BYBIT_API_KEY=tu_api_key
BYBIT_API_SECRET=tu_api_secret
BYBIT_BASE_URL=https://api.bybit.com
BYBIT_ACCOUNT_TYPE=UNIFIED
BYBIT_DEFAULT_LEVERAGE=20
ALLOWED_ORIGINS=http://localhost:3000,https://panel.tu-dominio.com
CONTROL_USER=panel_admin
CONTROL_PASSWORD=cambia_est0
# Tokens rotatorios (token@YYYY-MM-DD). Puedes mantener uno o varios en paralelo.
PANEL_API_TOKENS=panel_token@2025-12-31,nuevo_token
# (Opcional) Token unico legado si no usas rotacion
# PANEL_API_TOKEN=panel_token
# Activa proxy auth cuando Nginx ya valida Basic y envia X-Forwarded-User
TRUST_PROXY_BASIC=0
PROXY_BASIC_HEADER=X-Forwarded-User

# --- Loop de estrategias ---
STRATEGY_ID=scalp_rush_v1
STRATEGY_INTERVAL_SECONDS=30
STRATEGY_SERVER=http://127.0.0.1:8080
STRATEGY_LEVERAGE=20

# --- Panel Next.js ---
NEXT_PUBLIC_API_BASE=https://api.slstudominio.com
NEXT_PUBLIC_PANEL_API_TOKEN=panel_token
NEXT_PUBLIC_CONTROL_AUTH_MODE=browser

